{{- if and (eq .Values.mode "distributed") .Values.distributed.enabled (not .Values.runtime.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "observer.runtimeSecretName" . }}
  labels:
    {{- include "observer.labels" . | nindent 4 }}
type: Opaque
stringData:
  NATS_URL: {{ include "observer.runtime.natsUrl" . | quote }}
  POSTGRES_DSN: {{ include "observer.runtime.postgresDsn" . | quote }}
  MONGODB_URI: {{ include "observer.runtime.mongodbUri" . | quote }}
{{- end }}
